{
  "imports": {
    "@hono/hono": "jsr:@hono/hono"
  },
  "tasks": {
    "production": "deno run --config=config/deno.production.jsonc --allow-read --allow-net --allow-env --allow-write --watch src/server.ts",
    "check": "deno fmt --check && deno lint",
    "test": "deno test"
  }
}
